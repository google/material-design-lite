---
import { Prism } from "@astrojs/prism";
const { code, lang } = Astro.props;
---

<Prism code={code} lang={lang} />

<style>
  @import url(../styles/prisma.css);

  .codepen-button {
    top: 10px;
    right: 10px;
    cursor: pointer;
    text-align: right;
  }
</style>
